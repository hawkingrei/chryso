# Java Bindings (Skeleton)

This folder is reserved for Java bindings. The initial plan:
- Generate JNI headers for the C ABI in `ffi/`.
- Provide a minimal `Parser` class that returns serialized plans.

Implementation will follow once the C ABI stabilizes.
